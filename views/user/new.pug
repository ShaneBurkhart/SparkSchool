extends ../layouts/focus-modal


block content
    h1 Sign up for beta access
    p For a flat fee of $10 you can join our beta program to get access to new courses before anyone else.

    if error
        p= error

    - user = user || {}
    form(id='signup-form' action='/user/create' method='POST')
        .input-group.full-name-group
            label(for='full-name') Full Name
            input(
                type='text'
                id='full-name'
                name='full_name'
                placeholder='Ex. John Doe'
            )
        .input-group.email-group
            label(for='email') Email
            input(type='text' id="email" name='email' placeholder='user@domain.com' value=user.email)
        .input-group.password-group
            label(for='password') Password
            input(type='password' id="password" name='password' placeholder='********')
        button Get Beta Access
