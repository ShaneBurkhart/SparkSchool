version: '2.1'
services:
    app:
        build: .
    nginx:
        image: nginx
        ports:
            - 8080:80
        links:
            - app
        volumes:
            - ./nginx.conf:/etc/nginx/nginx.conf
